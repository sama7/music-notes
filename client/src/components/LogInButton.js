export default function LogInButton(props) {
    return (
        <a
            href="http://localhost:5000/login"
            className="btn btn-primary"
        >
            Log in
        </a>
    );
}
